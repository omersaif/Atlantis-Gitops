version: 3
projects:
- dir: .
  name: production
  workflow: production
  apply_requirements: [workflow]
workflows:
  production:
   plan:
     steps:
     - init
     - plan:
         extra_args: ["-var-file", "prod.tfvars"]
