version: 3
projects:
- dir: .
  name: production
  workflow: production
  apply_requirements: [approved]
workflows:
  production:
   plan:
     steps:
     - init
     - plan:
         extra_args: ["-var-file", "prod.tfvars"]
